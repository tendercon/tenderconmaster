<div style="margin-top: -60px">
  <div class="row"> 
  <%= form_tag(invite_sub_contractor_tenders_path, :html => { :multipart => true }) do |f| %>
      <input type="hidden" class="tender_id" name="tender_id" value="<%= params[:info_id] %>">
      <div class="col-md-12">
        <div class="text-center">
          <h1>INVITE PREFERRED SUBCONTRACTORS</h1>
          <h4>
            The subcontractors invited from Your Preferred Network will receive an email invitation to join this tender.
          </h4>

        </div>

        <div style="height: 50px"></div>
        <div class="savvy_div">
          <% if @hc_invites.present? %>
              <%= render :partial =>  "hc_invites/savvy" %>
          <% else %>
              <div class="text-center">
                <p style="font-size: 14px">
                  You currently have 0 subcontractors in your Preferred Subcontractor Network
                </p>
                <p style="font-size: 14px">
                  If you wish to invite your subcontractors to quote on  this tender, you can do so on the Subcontractor tab in Manage Tender
                </p>
              </div>
          <% end %>
        </div>
      </div>

      
      <div class="col-md-12">
        <div class="form-actions">
          <a href="/tenders/new_tender?package=true&info_id=<%= @tender.id %>"> 
            <button class="btn btn-default"  type="button">BACK</button>
          </a>   
          <a href="#!"> 
            <button class="btn btn-primary" type="submit">SAVE AND CONTINUE</button>
          </a>   

        </div>
      </div>

  <% end %>
</div> 
</div>


<script>
  $('.f_name').keyup(function(){
    var val = $(this).val();

    if(val.length > 0){

    }
  });

  $('.f_name').focusout(function(){
    var val = $(this).val();

    if(val.length > 0){

    }
  });



  function active_invite_btn(){

    $('.invite_submit_btn').prop('disabled',false);
  }


  $('.invite_btn').click(function(){
    var tender_id = $('.tender_id').val();
    $.ajax({
      method: "POST",
      data: {tender_id:tender_id},
      url: "/tenders/new_invite_form"
    }).done(function (data) {
      $('.new_invite').append(data);

    });
  });
</script>