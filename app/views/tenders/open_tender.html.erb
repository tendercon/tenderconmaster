<div class="col-md-22">
  <div class="col-md-22"> 
    <div class="col-md-20  col-md-offset-4">
      <h1 class="text-center">OPEN TENDERS</h1>
    </div>
    <div class="col-md-20" style="height: 80px;"></div>
    <!--<div class="row">
      <div class="form-group"></div>
    </div>
    <br><br>
    <%# if @plan.plan == 'STARTER PLAN $0' %>
      <div class="row">
        <div class="col-md-22"> 
          <div class="col-md-20  col-md-offset-10">
            <h3>Time till you can apply for 1 tender: <label id="open_tender_counter"></label></h3>
          </div>
        </div>
        <div class="col-md-22"> 
          <div class="col-md-20  col-md-offset-10">
            <a href="<%= subscription_users_path %>">
              <button class="btn btn-success">Upgrade Now</button>
            </a>
            and access ALL tenders immediately
          </div>
        </div>
      </div>
    <%# end %>
    <div class="form-group"></div>
    <br><br>
    <%# if @plan.plan == 'STARTER PLAN $0' && @range_month == false %>
      <div class="row">
        <div class="col-md-20  col-md-offset-4">
          <div class="col-md-5" style="background-color: gainsboro;">
            <h3 class="text-center">
              Tenders Available on Tendercon
            </h3>
            <h1 class="text-center" style="color: green"><%#= @tender_count %></h1>
          </div>
          <%# if @tender_trades_count > 0 %>
            <div class="col-md-5 col-md-offset-1" style="background-color: gainsboro;">
              <h3 class="text-center">
                Tender Matching your Trades
              </h3>
              <h1 class="text-center" style="color: green"><%#= @tender_trades_count %></h1>
            </div>
          <%# end %>

          <div class="col-md-5 col-md-offset-1" style="background-color: gainsboro;padding-right: 10px;padding-left: 10px;">
            <h3 class="text-center">
              Tender Applied Last 30 days
            </h3>

            <h1 class="text-center" style="color: green">1</h1>
          </div>

          <div class="col-md-5 col-md-offset-1" style="background-color: red;padding-right: 10px;padding-left: 10px;">
            <h3 class="text-center">
              Tender you can apply for <i class="fa fa-question-circle fa-1x" aria-hidden="true"></i>
            </h3>

            <h1 class="text-center" style="color: white">0</h1>
          </div>
        </div>
      </div>

    <%# else %>
      <div class="row">
        <div class="col-md-20  col-md-offset-4">
          <div class="col-md-7" style="background-color: gainsboro;">
            <h3 class="text-center">
              Tenders Available on Tendercon
            </h3>
            <h1 class="text-center" style="color: green"><%#= @tender_count %></h1>
          </div>
          <%#if @tender_trades_count > 0 %>
            <div class="col-md-7 col-md-offset-1" style="background-color: gainsboro;">
              <h3 class="text-center">
                Tender Matching your Trades
              </h3>
              <h1 class="text-center" style="color: green"><%#= @tender_trades_count %></h1>
            </div>
          <%#end %>

          <div class="col-md-7 col-md-offset-1" style="background-color: gainsboro;padding-right: 10px;padding-left: 10px;">
            <h3 class="text-center">
              Tender you can apply for <i class="fa fa-question-circle fa-1x" aria-hidden="true"></i>
            </h3>
            <h1 class="text-center" style="color: white;background-color: green">1</h1>
          </div>
        </div>
      </div>
    <%# end %>


    <br><br>-->
    <!--<div class="col-md-10  col-md-offset-8">
      <input type="text" class="form-control" id="inputSuccess2" style="border-radius: 5px"/>
      <span class="glyphicon glyphicon-search form-control-feedback"></span>
    </div>-->

    <br><br>

    <div class="row">
      <div class="col-md-6 col-md-offset-5">
        <!--<div class="dropdown" style="position:relative">
          <a href="#" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">Newest Tenders Published <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li>
              <a class="trigger right-caret">Value</a>
              <ul class="dropdown-menu sub-menu">
                <%# @values.each do |v| %>
                    <li><a href="#" class="filter"><%#= v.range %></a></li>
                <%# end %>
              </ul>
            </li>
            <li>
              <a class="trigger right-caret">Sector</a>
              <ul class="dropdown-menu sub-menu">
                <%# @sectors.each do |s| %>
                    <li><a href="#" class="filter"><%#= s.name %></a></li>
                <%# end %>
              </ul>
            </li>
            <li>
              <a class="trigger right-caret">Distance from me</a>
              <ul class="dropdown-menu sub-menu">
                <%# ['< 15kms','< 50kms','< 100kms','< 200kms','> 200kms'].each do |s| %>
                    <li><a href="#" class="filter"><%#= s %></a></li>
                <%# end %>
              </ul>
            </li>
            <li>
              <a class="trigger right-caret">Quotes Due</a>
              <ul class="dropdown-menu sub-menu">
                <%# ['< 24 hrs','< 7 days','> 7 days'].each do |s| %>
                    <li><a href="#" class="filter"><%#= s %></a></li>
                <%# end %>
              </ul>
            </li>
          </ul>
        </div>-->
        <!--<select multiple id="e1" style="width:396px">
          <optgroup label="Values">
            <%# @values.each do |v| %>
                <option><%#= v.range %></option>
            <%# end %>
          </optgroup>
          <optgroup label="Sector">
            <%# @sectors.each do |s| %>
                <option><%#= s.name %></option>
            <%# end %>
          </optgroup>
          <optgroup label="Disatance from me">
            <%# ['< 15kms','< 50kms','< 100kms','< 200kms','> 200kms'].each do |s| %>
                <option><%#= s %></option>
            <%# end %>
          </optgroup>
          <optgroup label="Quote Due">
            <%# ['< 24 hrs','< 7 days','> 7 days'].each do |s| %>
                <option><%#= s %></option>
            <%# end %>
          </optgroup>
        </select>-->
      </div>
      <!--<div class="col-md-8 col-md-offset-5">
        <a href="#!">
          <button class="btn btn-success search_filter">
            Search
          </button>
        </a>
      </div>-->
    </div>

    <div class="col-md-24  col-md-offset-2 tender_list_filter">
      <table  class="table table-bordered responsive" id="open_tender_table"  cellspacing="0" width="100%">
        <thead>
          <tr>
            <th>Tender ID</th>
            <th>Project Name</th>
            <th>Value</th>
            <th>Sector</th>
            <th>Location</th>
            <th>Distance from me</th>
            <th>Head Contractor</th>
            <th>Quote Due</th>
          </tr>
        </thead>
        <tbody class="searchable">
          <% if @tenders.present? %>
            <% @tenders.each do |t| %>
              <tr>
                <% if session[:role] == 'Head Contractor' %>
                  <td class="text-center">
                    <a href="/tenders/new_tender?info=true&info_id=<%= t.id %>">
                      <%= t.tendercon_id %>
                    </a>
                  </td>
                <% else %>
                  <td class="text-center">
                    <a href="#!" class="tender" id="<%= t.id %>">
                      <%= t.tendercon_id %>
                    </a>
                  </td>
                <% end %>

                <td class="text-center"><%= t.title %></td>
                <td class="text-center"><%= get_value_name t.tender_value_id %></td>
                <td class="text-center"><%= get_trade_category_name  t.category_id %></td>
                <% if t.address_terms.present? %>
                  <td class="text-center">
                    Hidden Address <i class="fa fa-question-circle" aria-hidden="true" title="Address will appear once your Head Contractor approved your request"></i>
                  </td>
                <% else %>
                    <td class="text-center"><%= t.address %></td>
                <% end %>
                <td class="text-center"><%= get_distance t.id,session[:user_logged_id] %></td>
                <td class="text-center"><%= get_user_company_name t.user_id %></td>
                <td class="text-center"><%= get_tender_due t.id %></td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
        </table>
    </div>
  </div>
</div>


<div class="upgrade_request ">
  <% if @tenders.present? %>
    <%  @tenders.each do |u| %>
      <div id="tender_Modal_<%= u.id %>" class="modal fade col-md-24" role="dialog" data-backdrop="static" data-keyboard="false" style="margin-top: 100px;" >
        <div class="modal-dialog">
          <!-- Modal content-->
          <div class="modal-content">
            <div class="modal-header" style="background-color: #184a5f">
              <button type="button" class="close" data-dismiss="modal">&times;</button>
              <div class="col-sm-24 col-md-14 col-md-offset-6">
                <% if @plan.plan == 'STARTER PLAN $0' && @range_month == false  %>
                  <h3 class="text-center" style="color: green">You reach your limit for this month. Upgrade plan to apply to all tenders.</h3>
                <% end %>
                <p class="text-center" style="font-size: 20px;color: white"><%= u.title %></p>
              </div>
              <div class="row">
                <div class="form-group"></div>
              </div>
            </div>
            <br>
            <div class="modal-body open_tender_body">
              <input type="hidden" class="tender_id" value="<%= u.id %>">
              <div class="row">
                <div class="container">
                  <div class="row">
                    <div class="col-md-12">
                      <button class="btn btn-default pull-right why_quote">
                        Why Should i quote to this Tender?
                      </button>
                    </div>
                  </div>
                </div>
                <div class="container">
                  <div id="content">
                    <ul id="tabs" class="nav nav-tabs" data-tabs="tabs">
                      <li class="active"><a href="#red<%= u.id%>" data-toggle="tab" id="<%= u.id %>">Details</a></li>
                      <li><a href="#orange<%= u.id%>" data-toggle="tab" id="<%= u.id %>">Trades</a></li>
                      <li><a href="#yellow<%= u.id%>" data-toggle="tab" id="<%= u.id %>">Documents</a></li>
                      <li><a href="#green<%= u.id%>" data-toggle="tab" id="<%= u.id %>">Map</a></li>
                      <li><a href="#blue<%= u.id%>" data-toggle="tab" id="<%= u.id %>">

                      </a></li>
                    </ul>
                    <div id="my-tab-content" class="tab-content">
                      <div class="row">
                        <div class="form-group"></div>
                      </div>

                      <div class="tab-pane active" id="red<%= u.id%>">
                        <div class="row">
                          <div class="form-group"></div>
                          <div class="col-md-9"></div>
                          <div class="col-md-6">
                            <img src="<%= get_user_company_avatar u.user_id %>" width="100px" height="100">
                          </div>
                        </div>
                        <div class="col-md-22">
                          <div class="col-md-4">
                            <label>Tender ID</label>
                          </div>

                          <div class="col-md-20">
                            <label><%= u.tendercon_id %></label>
                          </div>

                          <div class="col-md-4">
                            <label>Status</label>
                          </div>

                          <div class="col-md-20">
                            <% if u.status == '1' %>
                              <label>Tendering-The Head Contractor still tendering for this project.</label>
                            <% else %>
                              <label>Awarded-This tender has been awarded to the Head Contractor </label>
                            <% end %>
                          </div>

                          <div class="col-md-4">
                            <label>Client</label>
                          </div>

                          <div class="col-md-20">
                            <label><%= u.client.present? ? u.client : '-------' %></label>
                          </div>

                          <div class="col-md-4">
                            <label>Architect</label>
                          </div>

                          <div class="col-md-18">
                            <label><%= u.architect.present? ? u.architect : '-------' %></label>
                          </div>

                          <div class="col-md-4">
                            <label>Sector</label>
                          </div>

                          <div class="col-md-20">
                            <label><%= get_categoty_name u.category_id %></label>
                          </div>

                          <div class="col-md-4">
                            <label>Value</label>
                          </div>

                          <div class="col-md-20">
                            <label><%= get_value_name u.tender_value_id %></label>
                          </div>

                          <div class="col-md-4">
                            <label>Description</label>
                          </div>

                          <div class="col-md-20">
                            <label><%=  u.description.present? ? u.description.html_safe : '-------' %></label>
                          </div>
                          <% if u.address_terms != "on"  %>
                            <div class="col-md-4">
                              <label>Address</label>
                            </div>

                            <div class="col-md-20">
                              <label><%=  u.address %></label>
                            </div>
                          <% end %>

                          <div class="col-md-4">
                            <label>Distance from me </label>
                          </div>

                          <div class="col-md-20">
                            <label><%= get_distance u.id,session[:user_logged_id] %></label>
                          </div>

                          <% if (get_tender_sites u.id).present? %>
                            <% (get_tender_sites u.id).each do |a| %>
                              <div class="col-md-4">
                                <label>Site Inspection</label>
                              </div>
                              <div class="col-md-22">
                                <div class="col-md-4"></div>

                                <div class="col-md-10">
                                  <table class="table table-bordered">
                                    <thead>
                                    <tr>
                                      <th>Description</th>
                                      <th>Date</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                      <td><%= a.site_description %></td>
                                      <td><%= a.site_date %></td>
                                    </tr>
                                    </tbody>
                                  </table>
                                </div>
                              </div>
                              <br>
                            <% end %>
                          <% end %>
                          <div class="col-md-4">
                            <label>Quote</label>
                          </div>
                          <div class="col-md-22">
                            <div class="col-md-4"></div>
                            <div class="col-md-10">
                              <table class="table table-bordered">
                                <thead>
                                <tr>
                                  <th>Description</th>
                                  <th>Date</th>
                                </tr>
                                </thead>
                                <tbody>
                                <tr>
                                  <td><%= u.tender_quote.quote_description  %></td>
                                  <td><%= u.tender_quote.quote_date %></td>
                                </tr>
                                </tbody>
                              </table>
                            </div>
                          </div>
                        </div>
                      </div>
                      <div class="tab-pane" id="orange<%= u.id%>">
                        <div class="col-md-18">
                          <% @trade_categories.each do |tc|  %> 
                            <div class="<%= tc.title %>" id="<%= tc.title %>"> 
                              <% if(check_if_categories_has_trade u.id,tc.id) %>
                                <h1> <%= tc.title %></h1> 
                              <% end %>
                              <% if (get_tender_trades u.id).present? %>
                                <% (get_tender_trades u.id).each_with_index do |t,index| %>
                                  <% if tc.id.to_i == (get_trade_category_id t.trade_id).to_i %>
                                    <p style="font-size: 12px;font-weight: bold"><%= get_trade_name t.trade_id %></p>
                                  <% end %>
                                <% end %>
                              <% end %>
                            </div> 
                          <% end %> 
                        </div>
                      </div>
                      <div class="tab-pane" id="yellow<%= u.id%>">
                        <div class="col-md-22">
                          <div class="col-md-24 col-md-13 col-md-offset-0">
                            <p class="text-center">
                              <i class="fa fa-file fa-4x" aria-hidden="true"></i>
                            </p>
                          </div>
                          <br>
                          <% if !u.hide_document.present? || u.hide_document == '0'  %>
                            <div class="col-md-24 col-md-13 col-md-offset-0">

                              <% if (get_tender_document_package u.id).present? %>
                                <p class="text-center" style="font-size: 18px">
                                  <b>Head Contractor has uploaded documents</b>
                                </p>
                                <select onchange="location = this.options[this.selectedIndex].value;" class="form-control download">
                                  <option value="#!">None</option>
                                  <option value="<%= "http://"+@ip+"/assets/tender/document/Fullset-#{u.tendercon_id}.zip" %>">Full set</option>
                                  <% (get_tender_document_package u.id).each do |d| %>
                                    <option value="<%= d.url %>"><%= File.basename(d.url) %></option>
                                  <% end %>
                                </select>
                              <% else %>
                                <p class="text-center" style="font-size: 18px">
                                  <b>Head Contractor has not uploaded any documents yet</b>
                                </p>
                                <p class="text-center" style="font-size: 12px">
                                  Document will be available after your request has been approved
                                </p>
                                <p class="text-center" style="font-size: 12px">
                                  Press the [I AM INTERESTED] below to notify Head Contractor.
                                </p>
                              <% end %>
                            </div>
                          <% else %>
                            <div class="col-md-24 col-md-13 col-md-offset-0">
                              <p class="text-center" style="font-size: 18px">
                                <b>Head Contractor has hidden the documents</b>
                              </p>
                              <p class="text-center" style="font-size: 16px">
                                Documents will be available after your request approved
                              </p>
                            </div>
                          <% end %>
                        </div>

                      </div>
                      <div class="tab-pane" id="green<%= u.id%>">
                        <% if u.address_terms != 'on'  %>
                          <input type="hidden"  class="tenders" value="<%= u.id %>">
                          <input type="hidden"  class="add<%= u.id %>" value="<%= u.address %>">
                           <div id="map_<%= u.id %>" style="height:280px;width:570px;"></div>
                          <div class="col-md-24 col-md-13 col-md-offset-0">
                            <p class="text-center" style="font-size: 14px">
                              <b>Address: <%= u.address %></b>
                            </p>
                            <p class="text-center" style="font-size: 12px">
                             Post code: <%= u.postcode %>
                            </p>
                          </div>
                        <% else %>
                          <div class="col-md-24 col-md-13 col-md-offset-0">
                            <p class="text-center" style="font-size: 18px">Map is hide by the Head Contractor.</p>
                          </div>
                        <% end %>
                      </div>
                    </div>
                    <div class="quote_info" style="display: none;">
                      <div class="container">
                        <br/><br/><br/>
                        <div class="col-md-12">
                          <p class="text-center" style="font-size: 20px">
                            Why Should i quote to this Tender?
                          </p>
                          <br/><br/>
                          <p class="text-center" style="font-size: 14px">
                            Document will be available after your request has been approced
                          </p>
                          <p class="text-center" style="font-size: 14px">
                            Press the[I AM INTERESTED] below to notify Head Contractor
                          </p>
                          <br/><br/>
                          <p class="text-center" style="font-size: 14px">
                            to find out more <a href="/invites">UPGRADE</a> to Professional now.
                          </p>
                        </div>
                      </div>
                    </div>

                  </div>
                </div>
              </div>
              <div class="row">
                <div class="form-group"></div>
                <div class="col-sm-24 col-md-14 col-md-offset-9">
                  <%=  %>
                  <% if @range_month != nil %>
                    <% if @plan.plan == 'STARTER PLAN $0' && @range_month == false %>
                      <a href="<%= subscription_users_path %>">
                        <button class=" btn btn-info quotes" type="button">Upgrade Plan</button>
                      </a>
                    <% else %>
                      <a href="#!">
                        <button class=" btn btn-info interested_btn" type="button" id="<%= u.id %>" >I AM INTERESTED</button>
                      </a>
                    <% end %>
                  <% else %>
                    <a href="#!">
                      <button class=" btn btn-info interested_btn" type="button" id="<%= u.id %>" >I AM INTERESTED</button>
                    </a>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>



<div class="success">
  <% if @tenders.present? %>
    <%  @tenders.each do |u| %>
      <div id="tender_success_<%= u.id %>" class="modal fade " role="dialog" data-backdrop="static" data-keyboard="false" style="margin-top: 100px"   >
        <div class="modal-dialog">
          <!-- Modal content-->
          <div class="modal-content">
            <div class="modal-header" style="background-color: #184a5f">
              <button type="button" class="close" data-dismiss="modal">&times;</button>
              <div class="col-sm-24 col-md-14 col-md-offset-6">
                <p class="text-center" style="font-size: 20px;color: white"><%= u.title %></p>
              </div>
              <div class="row">
                <div class="form-group"></div>
              </div>
            </div>
            <br>
            <div class="modal-body">
              <div class="row">
                <div class="container">
                  <div class="col-sm-24 col-md-14 col-md-offset-5">
                    <i class="fa fa-check-circle fa-4x" aria-hidden="true" style="color: green"></i>
                  </div>
                  <div class="col-md-12">
                    <p class="text-center" style="font-size: 16px; font-weight: bold">
                      Congrats! Your request has been sent to the Head Contractor
                    </p>
                    <p class="text-center">
                      You will be notified when the HC has accepted or declined your request
                    </p>
                    <p></p>
                    <p class="text-center">Next Steps</p>
                    <p class="text-center">
                      1. This project will now be move to your MY TENDERS
                    </p>
                    <p class="text-center">
                      2. HC will get notified of your request
                    </p>
                  </div>
                </div>
              </div>
            </div>
            <div class="modal-footer" style="background-color: #184a5f">
              <div class="text-center">
                <a href="<%= open_tender_tenders_path %>">
                  <button type="button" class="btn btn-success">Open Tenders</button>
                </a>
                <a href="<%= my_tender_tenders_path %>">
                  <button type="button" class="btn btn-default">My Tender</button>
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

<script type="text/javascript" src="https://maps.google.com/maps/api/js?key=AIzaSyA7B-_hoQVA07h-qSd3E-HXxyXnM9W4zC8&libraries=places&sensor=false"></script> 
<script>
  var tender_id;
  var map;
  var tab_id;
  var myLatLng;

  <% if session[:role] == 'Sub Contractor' || session[:role] == 'Team Member' %>
    <% if @user_quote.present? %>
      var tender_date =  "<%= (@user_quote.created_at + 1.month).strftime("%Y-%m-%d") %>";

      console.log("tender_date:"+tender_date)
      $("#open_tender_counter").countdown(tender_date, function(event) {
        $(this).text(event.strftime('%D days %H hrs : %M mins : %S secs'));
    });
    <% end %>
  <% end %>

  $('#open_tender_table').DataTable({} );


  $('#inputSuccess2').keyup(function (){
    var rex = new RegExp($(this).val(), 'i');
    $('.searchable tr').hide();
    $('.searchable tr').filter(function () {
      return rex.test($(this).text());
    }).show();
  });

  $('.download').change(function(){
    var tender_id = $('.tender_id').val();
    var user_id = $('.sc_id').val();
    var trade =  $(this).val();

    $.ajax({
      method: "POST",
      data: {tender_id:tender_id,trade:trade,user_id:user_id},
      url: "/package_downloads/save_download"
    }).done(function (data){

    });
  });

  $('.interested_btn').click(function(){
    var id = $(this).attr('id');
    $.ajax({
      method: "POST",
      data: {tender_id:id},
      url: "/open_tenders/get_request_body"
    }).done(function (data){
      $('.open_tender_body').html(data);
    });
    //$('#tender_Modal_'+id).modal('hide');
    //$('#tender_trade_Modal_'+id).modal('show');
  });

  $('.quotes').click(function(){
    var tender_id = $('.tender_id').val();
    var sc_id = $('.sc_id').val();

    var ids = []
    $('.trade_class').each(function(){
      if($(this).is(':checked')){
        ids.push($(this).val());
      }
    });

    $.ajax({
      method: "POST",
      data: {tender_id:tender_id,sc_id:sc_id,ids:ids,open:true},
      url: "/tenders/request_to_quote"
    }).done(function (data) {
      $('#tender_trade_Modal_'+tender_id).modal('hide');
      $('#tender_success_'+tender_id).modal('show');
    });
  });

  $('.trade_class').change(function(){
    var ids = []
    $('.trade_class').each(function(){
      if($(this).is(':checked')){
        ids.push($(this).val());
      }
    });

    if(ids.length > 0){
      $('.quotes').prop('disabled',false);
    }else{
      $('.quotes').prop('disabled',true);
    }
  });

  $('.why_quote').click(function(){
    $('.quote_info').show();
    $('.tab-content').hide();
    $('li').removeClass("active");
  });

  $('a[data-toggle="tab"]').on('shown.bs.tab', function (e) {
    $('.tab-content').show();
    $('.quote_info').hide();
  });

  $(document).ready(function(){
    $('.tenders').each(function(){
      var id =  $(this).val();
      var add = $('.add'+id).val();
    });

    $('.tender').click(function(){
      var id = $(this).attr('id');
      var add = $('.add'+id).val();
      tender_id = id;
      tab_id = "#green"+tender_id;
      $.ajax({
        method: "POST",
        data: {add:add},
        url: "/tenders/get_latling"
      }).done(function (data) {
        $('#tender_Modal_'+id).modal('show');
        initialize(data,id);
      });
    });

    $('a').attr('href', tab_id).on('shown.bs.tab', function() {
      google.maps.event.trigger(map, 'resize');
    });


    function initialize(data,id) {
      myLatLng = {lat:data.latitude, lng: data.longitude};
      var mp =  'map_'+id;

      map = new google.maps.Map(document.getElementById(mp), {
        zoom: 15,
        center: myLatLng
      });

      var marker = new google.maps.Marker({
        position: myLatLng,
        map: map
      });

      google.maps.event.addListenerOnce(map, 'idle', function() {
        google.maps.event.trigger(map, 'resize');
        map.setCenter(myLatLng);
      });
    }

    $(function(){
      $(".dropdown-menu > li > a.trigger").on("click",function(e){
        var current=$(this).next();
        var grandparent=$(this).parent().parent();
        if($(this).hasClass('left-caret')||$(this).hasClass('right-caret'))
          $(this).toggleClass('right-caret left-caret');
        grandparent.find('.left-caret').not(this).toggleClass('right-caret left-caret');
        grandparent.find(".sub-menu:visible").not(current).hide();
        current.toggle();
        e.stopPropagation();
      });

      $(".dropdown-menu > li > a:not(.trigger)").on("click",function(){
        var root=$(this).closest('.dropdown');
        root.find('.left-caret').toggleClass('right-caret left-caret');
        root.find('.sub-menu:visible').hide();
      });
    });

    $('.search_filter').click(function(e){
      e.stopPropagation();
       console.log($("#e1").val())
      var filters = $("#e1").val();
      //$('.tender_list_filter').html('');
      $.ajax({
        method: "POST",
        data: {filters:filters},
        url: "/tenders/filter_open_tender"
      }).done(function (data) {
         $('.tender_list_filter').html(data);
      });
    });

    $("#e1").select2({
      "multiple": true,
      "closeOnSelect": false
    });
  })
</script>


<div class="requested">
  <% if @tenders.present? %>
      <%  @tenders.each do |u| %>
          <div id="tender_trade_Modal_<%= u.id %>" class="modal fade col-md-24"   style="margin-top: 100px;" >
            <div class="modal-dialog">
              <!-- Modal content-->
              <div class="modal-content col-md-24">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal">&times;</button>
                  <div class="col-md-24 col-md-20 col-md-offset-2">
                    <p class="text-center" style="font-size: 20px"><%= u.title %></p>

                  </div>
                  <div class="row">
                    <div class="form-group"></div>
                  </div>
                </div>
                <br>
                <input type="hidden" class="sc_id" value="<%= session[:user_logged_id] %>">
                <input type="hidden" class="tender_id" value="<%= u.id %>">
                <div class="modal-body" style="overflow-y: auto;height: 250px;">
                  <div class="row">
                    <div class="container">
                      <div id="content">
                        <div class="tab-pane" id="orange">
                          <div class="col-md-18">
                            <% @trade_cats = [] %>
                            <% @trade_categories.each do |tc|  %> 
                                <% if (get_tender_trades u.id).present? %>
                                    <% (get_tender_trades u.id).each_with_index do |t,index| %>
                                        <% if tc.id.to_i == (get_trade_category_id t.trade_id).to_i %>
                                            <div class="col-md-24"> 
                                              <% if tc.id.to_i == (get_trade_category_id t.trade_id).to_i %>
                                                  <% if !@trade_cats.include? tc.title %>
                                                      <% @trade_cats << tc.title %>
                                                      <h1> <%= tc.title %></h1> 
                                                  <% end %>
                                              <% end %>
                                            </div>
                                            <div class="col-md-22">
                                              <div class="col-md-6">
                                                <% if (get_open_tender_trades u.id,session[:user_logged_id]).present? %>
                                                    <% (get_open_tender_trades u.id,session[:user_logged_id]).each do |a| %>
                                                        <% if a == t.trade_id %>
                                                            <label id = "trade_id_<%= t.trade_id %>"><input type="checkbox" name="trade_lists[]"  value="<%= t.trade_id %>" class="trade_class" checked disabled> <%= get_trade_name t.trade_id %></label> 
                                                        <% else %>
                                                            <label id = "trade_id_<%= t.trade_id %>"><input type="checkbox" name="trade_lists[]"  value="<%= t.trade_id %>" class="trade_class"> <%= get_trade_name t.trade_id %></label> 
                                                        <% end %>
                                                    <% end %>
                                                <% else %>
                                                    <label id = "trade_id_<%= t.trade_id %>"><input type="checkbox" name="trade_lists[]"  value="<%= t.trade_id %>" class="trade_class"> <%= get_trade_name t.trade_id %></label> 
                                                <% end %>

                                              </div>
                                            </div>
                                        <% end %>
                                    <% end %>
                                <% end %> 
                            <% end %>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                  <div class="row">
                    <div class="form-group"></div>
                    <div class="col-sm-24 col-md-14 col-md-offset-8 ">
                      <% if (get_open_tender_trades u.id,session[:user_logged_id]).present? %>
                          <a href="#!">
                            <button class=" btn btn-info quotes" type="button">I AM INTERESTED</button>
                          </a>
                      <% else %>
                          <a href="#!">
                            <button class=" btn btn-info quotes" type="button" disabled>I AM INTERESTED</button>
                          </a>
                      <% end %>

                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
      <% end %>
  <% end %>
</div>